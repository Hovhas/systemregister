apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: systemregister
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/hovhas/systemregister
  ref:
    branch: main
  # För privat repo — skapa secret med GitHub token:
  #   kubectl -n flux-system create secret generic systemregister-github \
  #     --from-literal=username=git \
  #     --from-literal=password=<GITHUB_TOKEN>
  # secretRef:
  #   name: systemregister-github
